export interface LoginProps {
    username: string;
    password: string;
    rememberMe: boolean;
}

export interface SignupProps {
    name: string;
    username: string;
    email: string;
    password: string;
}